// rev.mjs — single source of truth for the release revision. Every generator that prints
// a revision imports THIS (F58 lesson: a stale rev string is a defect class, not a typo).
export const REV = "A.16";
