# Target bring-up checklist (silicon / RM / RTD / HIL / EOL)

This is the one list of what the host build cannot prove. Every open item of the firmware is here and
nowhere else (round 17): each row names what must be read in the S32K39 reference manual (RM) or bound
against the installed RTD / S32 Config Tools project, or measured on silicon, HIL or at EOL; the check that
closes it; and what holds the image closed until then. Contract decisions are in
`docs/firmware-contract.md` (§10c lists them); this page holds only what needs the hardware.

Every marker in the firmware sources has a row here, cited as `file:line`, and every `file:line` cited here
is a marker: `make target-check` fails when the two drift apart. Marker kinds: **RTD** bind against the RTD
release and the Config Tools project; **RM** read in the S32K39 RM; **HW-RM** read in the RM and confirm on
silicon; **HW** measure on the target; **EOL** an EOL/HIL station record; **REL** the release/product build.

**What always holds.** Arming needs the five FW-24 evidence items (route bound, configuration read back,
REG_PROT locked, and the EOL/HIL record for this `TI_FW_ID`, SKU and device UID) and a valid layout-2
calibration record; anything missing is a DTC, no FS0B release, no FW-16 energisation, no `MCU_GATE_EN`, and
INV_STATUS byte 15 names it. A target build with an unfilled board configuration does not compile. A
`make target-check` build (no RTD) returns the safe default from every HAL call and never arms.

| ID | Marker(s) | Item | What to read, bind or measure | Acceptance | Enforced today |
|---|---|---|---|---|---|
| T-01 | `src/platform/s32k396/s32k396_board_cfg.h:3` | README 2, FW-15/24 | RM SIUL2 IMCR table: the IMCR index of eFlexPWM_1 FAULT0 and FAULT2, and the SSS values selecting PTC26 (FLT_HS_N) and PTC25 (FLT_LS_N) | values filled; the EOL pad injection (T-05) reaches FAULT0/FAULT2; if the mux cannot reach them from these pads, a card pin swap (FW-15), not another firmware route | `#error` in a target build until filled; `_Static_assert` non-zero, two different IMCR indices; IMCRs read back at init and every 1 ms (ROUTE_BOUND); unbound ⇒ nothing written to any IMCR, no arming |
| T-02 | `src/platform/s32k396/s32k396.h:103` | README 2 | RM: width of the IMCR SSS field (`TI_IMCR_SSS_MASK` = 0xF assumed) | read-back of both IMCRs equals the filled SSS values on silicon | the route read-back compares under this mask; the EOL pad injection is the independent proof |
| T-03 | `src/platform/s32k396/s32k396_cfg.h:157`, `src/platform/s32k396/s32k396_pwm.c:49` | README 21 | RM REG_PROT: SLBR offset (module + 0x1800), GCR offset (+ 0x1FFC), HLB bit 31, WE shift 4 (the generic S32K3 layout); whether REG_PROT covers eFlexPWM_1 on the S32K39 — if not, XRDC makes the lock-down registers read-only for the application domain and `hal_pwm_protection_locked()` reads the XRDC state instead | on silicon a CPU and a DMA write to each locked register are rejected, the lock reads back after every reset (as the host model: `platform_cfg: protected_registers_reject_writes_from_cpu_and_dma`, `scenarios: watchdog_reset_relocks_before_rearming`) | PROTECTION_LOCKED only when HLB and every SLB bit read back set — never from the image, false in `target-check` builds; missing ⇒ DTC_PWM_LOCK, no arming |
| T-04 | `src/platform/s32k396/s32k396_pwm.c:51`, `src/platform/s32k396/s32k396_pwm.c:32` | README 21 | RM: SIUL2 register protection for the two IMCRs; whether REG_PROT locks per byte and eFlexPWM accepts byte writes (then lock CTRL2's upper byte — INDEP, INIT_SEL, DBGEN — and write FORCE as a byte) | both IMCRs locked and read back like the PWM registers; CTRL2 upper byte locked if byte locking exists | today the IMCRs are read back every 1 ms (a change while armed is "control lost") but not write-protected; CTRL2's INDEP is read back every 1 ms (CONFIG_MATCHES) |
| T-05 | `src/safety/arm_evidence.h:16` | README 22, HIL/EOL records | the EOL/HIL rig (not in this repository): with the CPU halted, drive each FLT pad low and see the eFlexPWM fault; inject a V_DC over-voltage and measure trip crossing → `ASC_REQ` edge; write `arm_validation_t` (`arm_validation_make/_seal`) for `TI_FW_ID` **0x0A0F0012** (round 18), the SKU and the device UID | both flags set, measured chain ≤ 15.6 µs (FW-06), a record per image and card; a new record for every new `TI_FW_ID` | FAULT_ROUTE_VALIDATED / OVP_ROUTE_VALIDATED only from a CRC-sealed record bound to this image, SKU and UID (`calib: validation_record_binds_image_card_and_crc`); none ⇒ DTC_ARM_EVIDENCE, no arming, status b15 names it |
| T-06 | `src/app/app.h:31` | README 29, HIL/EOL records | the release process derives `TI_FW_ID` from the build (today set by hand: 0x0A0F0012, round 18) | every image change changes it; the T-05 record exists for the released ID | a record for another ID validates nothing (`scenarios: arming_refused_on_record_identity_or_crc_mismatch`) |
| T-07 | `src/nvm/calib.h:18` | HIL/EOL records | the EOL station (not in this repository) measures and seals the FW-20 record, **layout 2**: current and V_DC gains/offsets per channel, resolver gains/offsets/phase trim and electrical zero, the excitation monitor's gain (codes per V pp) and the resolver's EOL ratio with the real harness and cold PTCs, motor data (L_d, L_q, ψ, R_s, n_max, `rule_b_released`), serial = device UID; with it `cal_rslv_wind_per_mon` from the harness (T-31) | `calib_check` = 0 on the card; at the trim band's low edge (0.95 × 7.2 V pp) the cold winding stays above the 6.5 V pp floor with no nuisance trip (FW-30) | missing, layout 1, CRC, range, SKU, serial or motor-ID failure ⇒ DTC_CALIB_INVALID, no torque, n_x = 0 (every §6 decision takes the n ≥ n_x column) |
| T-08 | `src/platform/s32k396/s32k396_adc.c:108` | README 2 | that the ADC analog watchdog → TRGMUX → LCU (OR) → eFlexPWM_1 FAULT1 route exists on the S32K39 and its latency (contract FW-05/FW-06: this is the route; FAULT1 → high sides only) | HIL: a phase-current over-threshold on each phase channel takes the high sides off within 2 PWM periods; a V_DC over-threshold on each channel gives `ASC_REQ` within 15.6 µs (the T-05 measurement) | OVP_ROUTE_VALIDATED from the T-05 record; the software OC check in the current-loop ISR is the backstop |
| T-09 | `src/platform/s32k396/s32k396_adc.c:215` | README 2 | RTD: the watchdog threshold field names; channel → threshold register (CWSELRn), enable (CWENRn) in the generated channel configuration; the watchdog event output enabled towards the LCU | the T-08 injections trip at the programmed codes (±1 LSB) on each of the five channels | as T-08 |
| T-10 | `src/platform/s32k396/s32k396_adc.c:232` | README 2 | RM: WTISR layout (bit 2k low, 2k+1 high threshold k) | the T-08 injections report the right channel: OV ⇒ the overvoltage row and DTC, OC ⇒ the overcurrent row | the hardware FAULT1 action does not depend on it; only the §6 row the fault ISR raises does |
| T-11 | `src/platform/s32k396/s32k396_adc.c:10` | README 27 | measure V_DC ch2's sampling gap with ADC1's injected chain (MT2_SIG, INTRLOK_N, TMOD_W) running: (1 + 3) conversions, 4 µs at the allocated 1 µs | gap ≤ 5.0 µs (FW-06 sample-wait row); otherwise move MT2_SIG off ADC1 | `platform_cfg: adc_schedule_follows_the_ball_map` checks the gap from the parameters; the T-05 record carries the measured whole chain (> 15.6 µs ⇒ no arming) |
| T-12 | `src/platform/s32k396/s32k396_adc.c:105` | README 28 | RM: BCTU LISTCHR layout, to read the conversion list back against the PHASE rows of the ball-map schedule (as NCMR/JCMR already are) | the read-back is implemented and refuses a list that does not convert ADC3_P1, ADC4_P5, ADC0_S19 | not read back yet: a wrong list shows only indirectly (the window, Σi and activity checks under current, the EOL current calibration) |
| T-13 | `src/platform/s32k396/s32k396_adc.c:22`, `src/platform/s32k396/s32k396_adc.c:30`, `src/platform/s32k396/s32k396_adc.c:99` | ADC | RTD: the ADC_SAR, BCTU, TRGMUX, LCU drivers and Config Tools symbols, `ADC_SAR_IP_CONV_CHAIN_*`; device-header names (IP_ADC_n, PCDR, ICDR, NCMR0/1, JCMR0/1, WTISR); per instance init + calibration, V_DC continuous, BCTU on PWM_1 VAL0/VAL1, the LCU OR | the target build links; `hal_adc_init()` returns true on silicon; phase triplets arrive at 2·f_sw | init reads every chain's NCMR/JCMR back against the ball map and refuses a stale project (`hal_adc_init` false ⇒ V_DC and the BCTU never start ⇒ SENSOR_SELFTEST times out ⇒ FAULT); a current loop that never runs is FW-31 |
| T-14 | `src/platform/s32k396/s32k396_adc.c:70` | ADC | RM: channel data register — VALID bit 19, CDATA[14:0] alignment (12-bit left-aligned in 15 bits unless MCR.DATA_ALIGN) | HW_ID, VOFS and V5GD read their nominal values on the card | a wrong VALID bit reads nothing new (stale ⇒ invalid); a wrong scaling leaves VOFS (0.475–0.525 V), V5GD and the HW_ID windows ⇒ no arming |
| T-15 | `src/platform/s32k396/s32k396_cfg.h:4` | PWM/ADC images | RM: every eFlexPWM and ADC_SAR field position of the register images (FCTRL, FSTS, DISMAP, OUTEN/MASK, DTSRCSEL/SWCOUT, CTRL/CTRL2, MCTRL, TCTRL; watchdog thresholds) against the RM revision in use | fields confirmed; `tests/test_platform_cfg.c` updated if one moves; FW-16 step h and T-05 pass; no false FLT trip on HIL with the fault-input glitch filter off (FFILT = 0) | CONFIG_MATCHES proves the registers hold the image, not that the fields mean what is assumed; FW-16 step h (FLT → FFLAG, DRV_EN_RB) checks the fault behaviour at every eligible key-on |
| T-16 | `src/platform/s32k396/s32k396.h:105` | GPIO | RM: SIUL2 MSCR OBE (21), IBE (19), SSS (2:0) | FW-16 step h drives the FLT pads through OBE on silicon; inputs read their levels | a wrong OBE fails step h (DTC_SELFTEST_FAIL, no arming); a dead IBE reads FLT low (no arming) |
| T-17 | `src/platform/s32k396/s32k396_io.c:133` | GPIO | RTD/RM: re-apply the REG_PROT soft lock on the FLT pads' MSCR after FW-16 step h | lock read back after step h | the step-h pad rule holds the data register at 0, so a stuck OBE can only pull FLT low (fail-safe) |
| T-18 | `src/platform/s32k396/s32k396_io.c:82` | GPIO | RTD: the generated Siul2_Port_Ip configuration leaves OBE off for the GPIO outputs (§9 step 1: latch first, then the buffer) | scope at power-up: no output glitches high, `ASC_CLR_N` never low | the pull-downs and FS0B keep DRV_EN low through reset whatever the pads do |
| T-19 | `src/platform/s32k396/s32k396.h:48` | PWM | RTD: `TI_PWM_CLK_HZ` = eFlexPWM_1's functional clock in the Clock_Ip configuration (160 MHz assumed) | scope: f_sw and dead time equal the SKU parameters (e.g. 10 kHz / 1.0 µs SiC, 5 kHz / 2.5 µs IGBT) | counts that do not fit the registers fail `hal_pwm_init` (CONFIG_MATCHES missing); a wrong but representable clock is **not** caught at run time — this scope check precedes the T-05 record |
| T-20 | `src/platform/s32k396/s32k396_pwm.c:17`, `src/platform/s32k396/s32k396_pwm.c:125`, `src/platform/s32k396/s32k396.h:64` | PWM | RTD: the eFlexPWM driver, `FlexPwm_Ip_UserCfg_1` (complementary pairs, centre-aligned signed counter, FULL + HALF reload, SM1/SM2 clocked, reloaded and forced from SM0, VAL0/VAL1 triggers to the BCTU); the device-header files and symbols (IP_FLEXPWM_1, SUB[] or SM[], IP_SIUL2, IP_STM_0) | scope: complementary, centred, dead time, trigger at both zero-vector centres | the counts and the lock-down image are written directly after the RTD init and read back (INDEP included); no trigger ⇒ no current loop ⇒ FW-31 |
| T-21 | `src/platform/s32k396/s32k396_main.c:18`, `src/platform/s32k396/s32k396_main.c:107` | clocks, IRQs | RTD: Clock_Ip (PLL, core, AIPS, eFlexPWM, SDADC, STM), Fccu_Ip (lockstep/DCM and ECC faults → FCCU_ERR0/1 → FS26 ERRMON), Siul2_Port_Ip; the Config Tools symbols; the IRQ numbers of the device header | clocks measured; an injected lockstep fault reaches the FS26; each ISR runs at its rate | a missing current-loop IRQ is FW-31; a missing fault IRQ leaves the hardware FAULT action but not the `ASC_REQ` edge, which the T-05 measurement then fails |
| T-22 | `src/platform/s32k396/s32k396_main.c:51` | IRQs | RTD: clear the BCTU list-complete flag | current-loop ISR at exactly 2·f_sw | an uncleared flag re-enters the ISR: the 1 ms task starves, the FS26 watchdog asserts FS0B |
| T-23 | `src/platform/s32k396/s32k396_main.c:58`, `src/platform/s32k396/s32k396_io.c:141` | time base | RTD: STM_0 prescaled to 1 MHz (`hal_time_us` is the raw counter), channel 0 compare = CNT + 1000 re-armed in its ISR | 1 ms task period and the µs base measured on a GPIO | a task that stops leaves the FS26 unanswered (FS0B within two windows) and the SWT expires; a wrong µs base moves the FS26 answers out of their window (FS0B) |
| T-24 | `src/platform/s32k396/s32k396_io.c:209` | watchdog | RTD: `Swt_Ip_Cfg0` timeout 50 ms, reset on timeout, keyed service | a stopped task resets the MCU after 50 ms (debug check) | the FS26 window watchdog (FW-12) is the independent one |
| T-25 | `src/platform/s32k396/s32k396_io.c:23` | NVM, SPI, CAN | RTD: Fee/Fls (MemIf types), STM/SWT/LPSPI/FlexCAN/Fee/Fls Config Tools symbols, the SIUL2 half-port symbols, the FeeBlockConfiguration numbers, the SPI timeout units, the FlexCAN DataInfo field names | every NVM record type survives a power cycle on silicon; FS26 frames are CRC-clean; CAN-FD frames on both buses | NVM records are CRC-checked (a wrong block map finds no calibration and no validation record ⇒ no arming); SPI errors are FS26 DTCs ⇒ no arming |
| T-26 | `src/platform/s32k396/s32k396_io.c:280`, `src/platform/s32k396/s32k396_io.c:305` | CAN | RTD: the FlexCAN callback and the RX message buffers: 0x101 and 0x102 on bus 0, the UDS request 0x7E1 on bus 1 (FW-32) | frames of all three IDs received at the bench | no VCU frame ⇒ never past VEHICLE_HANDSHAKE; no diagnostic frame ⇒ no service routine (the lock stays) |
| T-27 | `src/platform/s32k396/s32k396_main.c:67` | FW-20/24 | RTD/RM: the S32K39 unique ID (UTEST / DCM) as the 8-byte serial | unique per device, stable across resets, equal to the EOL station's reading | serial 0 fails `calib_check` (no torque); the validation record is bound to it |
| T-28 | `src/platform/s32k396/s32k396_resolver.c:24`, `src/platform/s32k396/s32k396_resolver.c:30`, `src/platform/s32k396/s32k396_resolver.c:54`, `src/platform/s32k396/s32k396_resolver.c:75` | resolver | RTD: the SDADC and eDMA drivers and symbols; per SDADC one eDMA channel (EXC, SIN, COS consecutive), four scatter-gather TCDs into the `.ti_nocache` ring, INTMAJOR on every TCD, its TCD destination-address read, error flag, INT-flag clear; SDADC inputs, decimation (ODR = 16 × carrier), trigger at the SWG period start, FIFO watermark 16 and overrun | frames of all three channels published at 10 kHz; a forced FIFO overrun or a stopped DMA yields no frame (the resolver ages, FW-28/29) | the frame protocol publishes nothing it cannot prove (a DMA position it cannot read ⇒ no frame ⇒ the resolver never validates ⇒ no arming); lost samples (`TI_SD_LOST`: the DMA error or FIFO overrun flag) keep the ring down until re-init — never re-acquired from the DMA positions, which cannot restore carrier phase 0 (round 18; `sdadc: lost_samples_keep_the_ring_down_until_init`) |
| T-29 | `src/platform/s32k396/s32k396_resolver.c:59` | resolver | measure the SDADC input range and gain needed for the windings | SIN/COS at the maximum excitation corner inside the SDADC range with margin | the amplitude window (sin² + cos²) and the range-checked resolver gains of the calibration record |
| T-30 | `src/platform/s32k396/s32k396_resolver.c:28` | SWG | RM: SGEN CTRL.IOFREQ for 10 kHz at the SWG clock | excitation measured at 10.000 kHz: the carrier period a whole 100 µs of STM counts (round 18: the resolver stamps are cadence-locked to it, T-40) | a wrong frequency breaks the 16-sample demodulation (amplitude/excitation checks) and `hal_swg_error()`: the resolver never validates |
| T-31 | `src/platform/s32k396/s32k396_resolver.c:115` | README 33 | RTD/RM: SGEN_1 CTRL (IOFREQ, IOAMPL, LDOS) and STAT (PHERR/FERR/SERR) symbols; the IOAMPL code → amplitude law — the datasheet (Table 40) gives MINAPP/MAXAPP only, `cal_swg_code_init` = 8 (≈ 1.45 V pp at the MAXAPP maximum corner) assumes it linear | SWG output measured at codes 0, 8, 12 and 15: code 8 starts below the slew-safe level at the maximum corner, the ramp reaches 7.2 V pp at the monitor; `cal_rslv_wind_per_mon` and the EOL ratio set with the real harness (T-07) | the trim closes on the monitor, not on the law; `ti_params_validate()` refuses a setpoint beyond the low corner, the slew ceiling or the cold floor; a trim at code 15 below its band is DTC_RSLV_SWG_SAT and nothing arms |
| T-32 | `src/safety/fs26.c:26` | README 11, FW-12 | silicon: the FS26 accepts the watchdog answer computed with a 32-bit intermediate and truncated to 16 bits; and the answer spacing against its window — 3 ms, first half closed, timed by the fail-safe oscillator (20 MHz ± 5 %): open 1.579–2.857 ms after the previous answer at every corner. Round 17 fixed the cadence (the answer first in the 1 ms task, due at ≥ 1500 µs: every second task, 1890–2110 µs by design, for an answer offset of 20–130 µs from the tick; before, ≈ 3.0 ms, the window's end) | the WD error counter stays 0 over 10 000 refreshes; the answer spacing (scope on the SPI CS) within 1890–2110 µs under the worst interrupt load, which also confirms the offset assumed (T-36) | `scenarios: fs26_is_answered_every_2ms_inside_its_window_at_both_oscillator_corners` on the target's exact tick grid at −5 / 0 / +5 %; a wrong or late answer counts WD errors: FS0B asserts at WD_ERR_LIMIT = 2 (DRV_EN low: SPO) and is released only once FLT_ERR_CNT is back to 0 |
| T-33 | `src/safety/fs26.c:158` | README 11 | silicon: the FS26 after an MCU-only reset (INIT_FS not re-entered, the INIT values held) and the FS0B/FS1B release after an RSTB (FLT_ERR_CNT counted down by good answers) | reset the MCU alone with the FS26 in NORMAL: the INIT values read back; FS0B released within `cal_sensor_selftest_ms` + `cal_fs0b_release_ms` | any INIT read-back mismatch ⇒ DTC, no arming; no release in time ⇒ FAULT (host-tested on the FS26 model: `fs26` suite) |
| T-34 | `src/safety/fs26.c:133` | README 6, EOL | EOL: read the FS26 OTP bank in debug/OTP mode and compare every field with design-basis §8a (GPIO1 push-pull, not slotted; FS1B_TDELAY 0; …) | all fields equal; `cal_fs26_prog_id` set to the procured variant's M_PROGID | at every boot: M_PROGID (0xFFFF unbound ⇒ no arming), OTP_CORRUPT, DBG_MODE and the INIT registers with their complements (FW-12) |
| T-35 | `src/app/app.c:30` | README 19 | REL: the product build defines `TI_UDS_KEY_FN` (the OEM SecurityAccess algorithm, e.g. a MAC in the HSE) and draws the seed from the HSE TRNG (the default seed is unique, not random) | a service tool unlocks with seed/key and the FW-32 routine clears a stuck-on-QDIS lock on the bench (HV absent): NVM record CLEARED with the key cycle, DTC_SERVICE_LOCK_CLEARED, the next power-up arms | no key ⇒ every seed request NRC 0x22 ⇒ the routine NRC 0x33 ⇒ the lock stays (`uds: security_access_refused_without_a_key_function`, `scenarios: service_lock_clear_is_refused_without_a_key`) |
| T-36 | — | timing | the WCET and latency columns of `docs/timing.md` at the worst operating point (RUN at the field-weakening limit, CAN at full load, an NVM job in flight): fault ISR, current-loop ISR (< 0.5·T_sw minus the conversion time), 1 ms task, critical sections; the FS26 answer's offset from its tick (≤ 130 µs assumed, FW-12 / T-32); the `ASC_REQ` re-edge pulse width; the ASC exit edge (the low-side ASC pins' release after the `ASC_CLR` falling edge ≤ 1.07 µs and the first high-side pulse after `cal_asc_release_ns` + the dead time, FW-06a step 3) | every budget of `docs/timing.md` met | the FW-06 chain is measured in the T-05 record; FW-31 catches a current loop that stops; the FS26 and SWT watchdogs catch a task that overruns |
| T-37 | — | commissioning | the 76 `cal_*` values (`include/cal_ranges.h`) that the contract does not fix: RDY timings, FW-16 chain timings and the one-shot, `cal_desat_en_hold_us` against the measured soft turn-off, `cal_rslv_latency_us` (sign convention, round 18 / FW-36: **positive = the reported angle lags the rotor** — the block's angle is the rotor's that long before its mid-block reference, and the firmware ADDS it when it extrapolates to the current loop; measure with the default 0 against a reference encoder at two speeds in both directions: L = (θ_encoder − θ_reported)/ω_e at the same instant, the same in both directions — a part that changes sign with the direction is an electrical-zero error, not latency; the value is the net delay of the whole chain relative to the mid-block reference, the demodulator's carrier weighting included — if it comes out negative the CAL floor 0 must be revisited in the contract), `cal_sd_irq_lat_max_us` (T-40), the resolver phase compensation, `cal_rslv_wind_per_mon`, DC-link trim gains on the real bank, precharge τ, thermal derating and `cal_peak_recovery_s`, `cal_vdyn_reserve_frac` on the dyno, activity thresholds, the LV bands (`cal_vsup_*`) against the OEM's LV profiles (IR-02/IR-03); with them the discharge τ per bank and the HW_ID divider on real cards | each value set from its measurement on the vehicle's motor, bank and harness | the contract default until then; every value range-checked at every boot (`ti_params_validate` ⇒ DTC_PARAMS_INVALID, no arming) |
| T-38 | — | vehicle integration | the DBC and E2E DataIDs of VCU_CMD, VCU_BMS and INV_STATUS (bytes 14–15 included); the rule (b) evidence for the motor/vehicle (`rule_b_released` in the calibration record); the BMS accepting the rectified charge (FW-08b); the HVIL harness signatures; the motor-temperature sensor curves | HIL/vehicle sign-off per contract §6 rule (b) and FW-08b | a frame layout or DataID mismatch fails the E2E check (no fresh command ⇒ no arming); `rule_b_released` defaults to false (no rule (b) credit: SPO where rule (a) fails becomes LS-ASC or the energy DTC) |
| T-39 | — | FW-33 | bench: the VSUP reading through the FS26 AMUX (M_AMUX_CTRL = VSUP / 14, written at every boot; SBC_AMUX on ADC0_S14) against a bench supply at 13.5, 20, 24, 27 and 35 V; then, running, the let-through profiles: ISO 16750-2 test B (35 V, 400 ms, five pulses a minute apart) and a 24 V / 60 s jump start | the reading within ± 1.5 % + 0.1 V of the supply (DS Table 131); each pulse and the jump start leave RUN and the torque untouched, with DTC_LV_OVERVOLTAGE stamped over the event; 35 V held past `cal_vsup_ld_ms` ramps the torque to zero with DTC_LV_OV_SUSTAINED and it returns when KL30 does | host: the AMUX modelled (`scenarios: lv_*`); no reading (the AMUX unconfigured: its pin pulled to ground, Table 56) is DTC_LV_VSUP_UNKNOWN — supervision off, information only; the hardware carries the profiles either way (verification report, test-B rows) |
| T-40 | `src/platform/s32k396/s32k396_resolver.c:80` | FW-35 (round 18, A16-R02) | measure the latency of each SDADC completion interrupt (the DMA major-loop completion → entry of `s32k_sdadc_dma_irq`, e.g. a GPIO set in the TCD's last minor loop or the DONE bit polled against a GPIO set at handler entry) and its distribution over ≥ 10 min at the T-36 worst operating point, the fault ISR exercised; and confirm the cadence the stamps rely on: the SDADC data rate and the STM microsecond timer derive from the same PLL, the carrier period is a whole 100 µs of STM counts (T-30) — the cadence stamp of each frame against a GPIO at the SWG period start stays within ± 1 µs over an hour | the maximum stays below `cal_sd_irq_lat_max_us` (30 µs, range 5–45) with margin; over an hour of RUN, `DTC_RSLV_REACQUIRED` has no occurrence (a cadence that drifts against the timer would re-acquire at a steady rate) | a first completion served later than the bound is never published (the ring re-acquires, `DTC_RSLV_REACQUIRED`; frames absent past `cal_rslv_hold_us` take the FW-28 path), so a bound set too low costs availability, never a wrong stamp; a lap is caught at any bound in range; residual: the origin carries the latency of the completion that anchored it until a prompter one moves it back (a latency constant from the anchor on is indistinguishable from the cadence) — this measurement bounds it |
